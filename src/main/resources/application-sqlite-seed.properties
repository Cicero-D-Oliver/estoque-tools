# Perfil opcional. Use junto de sqlite: --spring.profiles.active=sqlite,sqlite-seed
# O callback é idempotente, não altera o histórico versionado e nunca é carregado
# pelos profiles SQLite base ou PostgreSQL.
spring.flyway.locations=classpath:db/migration/sqlite,classpath:db/seed/sqlite
